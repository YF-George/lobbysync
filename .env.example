# === 資料庫 (Supabase / PostgreSQL) ===
# 格式: postgresql://[user]:[password]@[host]:[port]/[db_name]
# 建議使用 Supabase 提供的 Transaction mode URL (port 6543)
DATABASE_URL="postgresql://postgres:你的密鑰@db.你的專案.supabase.co:6543/postgres"

# === Lucia Auth (身份驗證) ===
# 隨機字串，可用 `openssl rand -base64 32` 生成
AUTH_SECRET="your-very-secure-random-secret"

# === Supabase 管理員 (用於後端同步 API) ===
# 到 Supabase -> Project Settings -> API 取得
SUPABASE_URL="https://你的專案.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="你的-service-role-key"

# === PartyKit 連接配置 ===
# 本地開發：localhost:1999 | 正式環境：你的-project.你的-username.partykit.dev
PUBLIC_PARTYKIT_HOST="localhost:1999"

# === 網站網址 (供 PartyKit 反向呼叫 API 用) ===
# 本地開發：http://localhost:5173
SVELTEKIT_URL="http://localhost:5173"